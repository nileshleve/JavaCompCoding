import java.io.*;
import java.util.*;

class accessory{
    public static void main(String... a)throws IOException{
        BufferedReader dis = new BufferedReader(new InputStreamReader(System.in));
        int t = Integer.parseInt(dis.readLine());
        while(t-- > 0){
            
            String st1[] = dis.readLine().split(" ");
            //String st1 = st.split(" ");
            System.out.println(st1[0]);
            // int L = Integer.parseInt(st1[0]);
            // int A = Integer.parseInt(st1[1]);
            // int N = Integer.parseInt(st1[2]);
            // int D = Integer.parseInt(st1[3]);

            // if(D < A){
            //     System.out.println("SAD");
            //     continue;
            // }
            // else if(D == A){

            // }
            // else{

            // }
        }
    }
}